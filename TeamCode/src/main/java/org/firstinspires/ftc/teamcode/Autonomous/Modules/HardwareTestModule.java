package org.firstinspires.ftc.teamcode.Autonomous.Modules;

import com.qualcomm.robotcore.hardware.DcMotor;

import org.firstinspires.ftc.teamcode.FTC_API.Autonomous.Modules.Module;
//import org.firstinspires.ftc.teamcode.Robot.SubSystems.HardwareTest;


/*
public class HardwareTestModule extends Module {
    private HardwareTest hardware;
    private boolean isDone = false;


    @Override
    public void start() {
        hardware = (HardwareTest) robot.getSubSystem("HardwareTest");
        hardware.setMotorSpeed(0.5);
        hardware.setMotorTargetPosition(1);
    }

    @Override
    public void tick() {
        if(!hardware.getMotor().isBusy()){
            isDone = true;
        }
    }

    @Override
    public int stop(){
        return positionInArray;
    }

    @Override
    public boolean isDone() {
        return isDone;
    }
}

*/
